/*
Package clientutil provides higher-level utility for github.com/docker/docker/client.

Note that unlike REST API nor CLI, the API of the clientutil package can change easily.
So users should vendor the clientutil package into their own repository.
*/
package clientutil
