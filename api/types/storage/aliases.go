package storage

import "github.com/moby/moby/api/types/storage"

// DriverData Information about the storage driver used to store the container's and
// image's filesystem.
type DriverData = storage.DriverData
