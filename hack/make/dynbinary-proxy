#!/usr/bin/env bash
set -e

[ -z "$KEEPDEST" ] && rm -rf "$DEST"

(
	if [ -n "$DEST" ] && [ -z "$OUTPUT" ]; then
		OUTPUT="$(dirname "$DEST")/dynbinary"
		export OUTPUT
	fi
	export DOCKER_LINKMODE="dynamic"
	export PACKAGE=./cmd/docker-proxy
	. hack/make/.binary
)
