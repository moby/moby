#!/usr/bin/env bash
set -e

[ -z "$KEEPDEST" ] && rm -rf "$DEST"

(
	if [ -n "$DEST" ] && [ -z "$OUTPUT" ]; then
		OUTPUT="$(dirname "$DEST")/binary"
		export OUTPUT
	fi
	export PACKAGE=./cmd/dockerd
	. hack/make/.binary
)
